Pre-Requists
  - Mathematics & Statistics
  - Programming skills
  - Data handling
  
History:
  -
  -  
ML Core compoents:
  - Dataset
  -  
ML Types
  - Supervised 
  - UnSupervised
  - Reinforcement
Deep Learning
  - 

Pipeline
  - Data colletion
  - Pre-processing
  - Feature Engineering
  - Model Training
  - Evaluation
  - Deployment

Words:
 Inputaion - fill in a reasonal estimate or remove those values
 skewed data - which direction the cureve is going

Data pre-processing
  components:
    Data Quality
    Missing Data - Remove column or row based on column importance
    Detecting Outliers - Identifying data points that are significantly different from the rest of the dataset
          - Z-score (standar score) - meansures how many deviations a values is way from the mean z= (x- mean) / standard deviation   > 3 (we can consider as outlier)
          - Interquartile Range = IQR = Q3-Q2   (Q3 = (75-n) / n-1 , Q2= (25-n)/n-1)) , identify position (take lower + 
          - Winsorization Visualizer - (lower fench = Q1-1.5*IQR and lower fench = Q2-1.5*IQR)  
        
    Encoding
    Feature Scaling
        - Min-Max scalling - (x - xmin/ xmax - xmin) Gives the range between [0,1] 
        - Z-Score Normalization z = (x-mean) / std
    Splitting Data
      -  Training Dataset
      - Validation Dataset
      - Test Dataset
    

  Features:
  - Rasw Data
  - Clean
  - Encode
  - Scale
  - Split
  - Ready for ML

Supervised learning
  Regression - How many / How many
      Types
        - Liner Regression
        - Logistc Regression
      Model
        -  Simple Linear Regression
        -  Multi Linear Regression

  Evaluation/Error metrics
      - Mean abosulate Error (MAE)
      - Means Squared Error (MSE)
      - Root mean squared Error (RMSE)
      - R-Square
      - Adjusted R2

  Correlation - Pearson correlation formula
      - Strength and direction of linerar relationship. r=(+/-1)

   Logistic Regression:
      - Is not regression, instead of a number predicts a probability (0 or 1) that an outcome belongs to a class. 

  classification - What type /category
  
